#ifndef EJOVO_ASSERT
#define EJOVO_ASSERT


// Let's add some assert macros, taking in an expression that returns is supposed to return true


#define rassert(expr) if (!(expr)) exit(2)














#endif